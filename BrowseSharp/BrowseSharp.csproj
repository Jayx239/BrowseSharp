<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <Authors>Jason Gallagher</Authors>
    <Description>A headless browser supporting web navigation, html parsing, css style parsing, and javascript parsing/execution.</Description>
    <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
    <AssemblyVersion>0.0.0.0</AssemblyVersion>
    <FileVersion>0.0.0.0</FileVersion>
    <Version>0.0.2-alpha</Version>
    <PackageLicenseUrl>https://github.com/Jayx239/BrowseSharp/blob/master/LICENSE</PackageLicenseUrl>
    <Copyright>Copyright (c) 2018 Jason Gallagher</Copyright>
    <PackageProjectUrl>https://github.com/Jayx239/BrowseSharp</PackageProjectUrl>
    <RepositoryUrl>https://github.com/Jayx239/BrowseSharp</RepositoryUrl>
    <RepositoryType>Git</RepositoryType>
    <PackageTags>Headless Browser Javascript HTML Parser Web</PackageTags>
    <Company>Jason Gallagher</Company>
    <PackageReleaseNotes>
- Added Features
Reworked browser entirely, no longer extends rest sharp client. Unit tests are passing for all Browser methods with the exception of the post methods. These will be tested before the next release.
This version should be used over all previous releases, future versions will be backward compatible with this version.

1. Documents 
  * Store Request Data
  * Stores Response Data
  * Stores javascripts
  * Stores css styles
2. Browser
  * Utilizes the RestSharp RestClient
  * Generates Docuements for each web request.
  * Supports asynchronous and synchronous
3. Javascript Engine
  * Used by the browser to scrape css styles from inline styles and linked styles.
  * Scraping logic is implemented for asynchronous and synchronous browser requests
4. StyleEngine
  * Used by browser to scrape JavaScript content from inline scripts and externally linked scripts.
  * Scraping logic is implemented for asynchronous and synchronous browser requests

Currently Under Development, more to come...</PackageReleaseNotes>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="AngleSharp" Version="0.9.9.2" />
    <PackageReference Include="Jint" Version="2.11.55" />
    <PackageReference Include="NETStandard.Library" Version="2.0.3" />
    <PackageReference Include="RestSharp" Version="106.3.1" />
  </ItemGroup>
</Project>